import { Canvas, Story, Meta, ArgsTable } from "@storybook/addon-docs";

import Card from "./Card";

<Meta title="Atoms/Card" component={Card} />

export const Template = (args) => <Card {...args} />;

# Card

The Card component wraps children, providing padding and a styled border.

<Canvas>
  <Story name="Card" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>
