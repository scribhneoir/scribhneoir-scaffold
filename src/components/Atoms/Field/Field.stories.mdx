import { Canvas, Story, Meta, ArgsTable } from '@storybook/addon-docs';

import Field from './Field';

<Meta
  title='Atoms/Field'
  argTypes={{ onChange: { action: 'textChanged' } }}
  component={Field}
/>

export const Template = (args) => <Field {...args} />;

# Field

<Canvas>
  <Story
    name='Field'
    args={{ placeholder: 'Field', value: '', disabled: false, error: '' }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Disabled'
    args={{
      placeholder: 'Field',
      value: 'Field',
      disabled: true,
      error: '',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Error'
    args={{
      placeholder: 'Field',
      value: 'Field',
      disabled: false,
      error: 'Error',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Field} />
