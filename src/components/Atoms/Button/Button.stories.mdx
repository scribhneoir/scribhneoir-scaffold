import { Canvas, Story, Meta, ArgsTable } from '@storybook/addon-docs';

import Button from './Button';

<Meta
  title='Atoms/Button'
  argTypes={{ onClick: { action: 'clicked' } }}
  component={Button}
/>

export const Template = (args) => <Button {...args} />;

# Button

The button component wraps children, and takes an onClick function as well as
primary and light styling booleans

<Canvas>
  <Story
    name='Primary'
    args={{ primary: true, children: 'Button' }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Secondary'
    args={{ primary: false, children: 'Button' }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Light'
    args={{ light: true, children: 'Button' }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Button} />
