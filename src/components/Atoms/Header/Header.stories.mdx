import { Canvas, Story, Meta, ArgsTable } from '@storybook/addon-docs';

import Header from './Header';

<Meta
  title='Atoms/Header'
  argTypes={{ onChange: { action: 'HeaderChanged' } }}
  component={Header}
/>

export const Template = (args) => <Header {...args} />;

# Header

<Canvas>
  <Story
    name='Header'
    args={{ children: 'Header' }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='H2'
    args={{
      children: 'Header',
      h: 2,
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='H3'
    args={{
      children: 'Header',
      h: 3,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Header} />
